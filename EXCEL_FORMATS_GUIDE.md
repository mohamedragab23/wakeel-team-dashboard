# 📋 دليل تنسيقات ملفات Excel

## ✅ التنسيقات المدعومة

النظام يدعم الآن **اكتشاف تلقائي للأعمدة** من العناوين، مما يعني أن الترتيب ليس ضرورياً تماماً. لكن التنسيقات التالية هي الموصى بها:

---

## 1. ملف المناديب (`ملف المناديب.xlsx`)

### التنسيق الموصى به:
| كود المندوب | الاسم | المنطقة | كود المشرف |
|-------------|-------|---------|------------|
| R001 | أحمد محمد | المنطقة الشمالية | S005 |
| R002 | سارة علي | المنطقة الجنوبية | S001 |

### العناوين المدعومة:
- **كود المندوب**: `كود المندوب`, `كود`, `رider code`
- **الاسم**: `الاسم`, `اسم`, `name`
- **المنطقة**: `المنطقة`, `منطقة`, `region`
- **كود المشرف**: `كود المشرف`, `مشرف`, `supervisor code`

### المتطلبات:
- ✅ كود المندوب **يجب أن يكون فريداً**
- ✅ كود المشرف **يجب أن يكون موجوداً** في النظام
- ✅ الاسم مطلوب
- ✅ المنطقة اختيارية

---

## 2. ملف الديون (`ملف الديون.xlsx`)

### التنسيق الموصى به:
| كود المندوب | المديونية |
|-------------|-----------|
| R001 | 1500 |
| R002 | 750 |

### العناوين المدعومة:
- **كود المندوب**: `كود المندوب`, `كود`, `rider code`
- **المديونية**: `المديونية`, `مبلغ`, `دين`, `amount`, `debt`

### المتطلبات:
- ✅ كود المندوب مطلوب
- ✅ المبلغ يجب أن يكون رقماً
- ✅ إذا كان المبلغ غير صحيح، سيتم اعتباره 0

---

## 3. ملف بيانات الأداء (`ملف بيانات الأداء.xlsx`)

### التنسيق الموصى به:
| التاريخ | كود المندوب | ساعات العمل | البريك | التأخير | الغياب | الطلبات | معدل القبول | المحفظة |
|---------|-------------|-------------|--------|---------|--------|---------|-------------|----------|
| 2024-01-15 | R001 | 8.5 | 2 | 15 | لا | 25 | 95% | 1200 |
| 2024-01-15 | R002 | 7.0 | 1 | 0 | لا | 20 | 98% | 500 |

### العناوين المدعومة:
- **التاريخ**: `التاريخ`, `date`
- **كود المندوب**: `كود المندوب`, `كود`, `rider code`
- **ساعات العمل**: `ساعات العمل`, `ساعات`, `hours`, `عمل`
- **البريك**: `البريك`, `بريك`, `break`
- **التأخير**: `التأخير`, `تأخير`, `delay`
- **الغياب**: `الغياب`, `غياب`, `absence`
- **الطلبات**: `الطلبات`, `طلبات`, `orders`, `طلب`
- **معدل القبول**: `معدل القبول`, `قبول`, `acceptance`, `معدل`
- **المحفظة**: `المحفظة`, `مديونية`, `debt`, `wallet`

### المتطلبات:
- ✅ التاريخ: يمكن أن يكون تاريخ Excel أو نص (سيتم تحويله تلقائياً)
- ✅ كود المندوب مطلوب
- ✅ جميع الأرقام يجب أن تكون أرقاماً
- ✅ الغياب: `نعم` أو `لا`
- ✅ معدل القبول: يمكن أن يكون `95%` أو `95`

---

## 🔍 كيف يعمل الاكتشاف التلقائي؟

النظام يبحث في **الصف الأول** (العناوين) عن الكلمات المفتاحية التالية:

### للمناديب:
- يبحث عن `كود` + `مندوب` → كود المندوب
- يبحث عن `اسم` → الاسم
- يبحث عن `منطقة` → المنطقة
- يبحث عن `كود` + `مشرف` → كود المشرف

### للديون:
- يبحث عن `كود` + `مندوب` → كود المندوب
- يبحث عن `مديونية` أو `مبلغ` أو `دين` → المبلغ

### لبيانات الأداء:
- يبحث عن `تاريخ` → التاريخ
- يبحث عن `كود` + `مندوب` → كود المندوب
- يبحث عن `ساعات` أو `عمل` → ساعات العمل
- ... وهكذا

**إذا لم يتم العثور على العناوين، يستخدم النظام المواضع الافتراضية (0, 1, 2, ...)**

---

## ⚠️ ملاحظات مهمة

1. **الصف الأول يجب أن يكون العناوين** (سيتم تخطيه)
2. **الصفوف الفارغة سيتم تخطيها تلقائياً**
3. **الأخطاء ستظهر في رسالة الخطأ** مع رقم الصف
4. **التحذيرات ستظهر في رسالة النجاح**

---

## ✅ أمثلة على الملفات المرفقة

تم إرفاق 3 ملفات مثال في المشروع:
- `ملف المناديب.xlsx`
- `ملف الديون.xlsx`
- `ملف بيانات الأداء.xlsx`

يمكنك استخدامها للاختبار! 🎉

